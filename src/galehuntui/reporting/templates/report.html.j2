<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Security Assessment Report - {{ run.target }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background: #f5f5f5;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            border-radius: 8px;
            overflow: hidden;
        }
        
        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px;
        }
        
        header h1 {
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 600;
        }
        
        header .subtitle {
            font-size: 16px;
            opacity: 0.9;
        }
        
        .content {
            padding: 40px;
        }
        
        .section {
            margin-bottom: 40px;
        }
        
        .section-title {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #667eea;
            color: #2d3748;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .info-card {
            background: #f7fafc;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }
        
        .info-card .label {
            font-size: 12px;
            text-transform: uppercase;
            color: #718096;
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        .info-card .value {
            font-size: 20px;
            font-weight: 600;
            color: #2d3748;
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            background: #f7fafc;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            border: 2px solid #e2e8f0;
        }
        
        .stat-card.critical {
            border-color: #c53030;
            background: #fff5f5;
        }
        
        .stat-card.high {
            border-color: #dd6b20;
            background: #fffaf0;
        }
        
        .stat-card.medium {
            border-color: #d69e2e;
            background: #fffff0;
        }
        
        .stat-card.low {
            border-color: #38a169;
            background: #f0fff4;
        }
        
        .stat-card.info {
            border-color: #3182ce;
            background: #ebf8ff;
        }
        
        .stat-card .count {
            font-size: 36px;
            font-weight: 700;
            margin-bottom: 5px;
        }
        
        .stat-card.critical .count { color: #c53030; }
        .stat-card.high .count { color: #dd6b20; }
        .stat-card.medium .count { color: #d69e2e; }
        .stat-card.low .count { color: #38a169; }
        .stat-card.info .count { color: #3182ce; }
        
        .stat-card .label {
            font-size: 12px;
            text-transform: uppercase;
            color: #718096;
            font-weight: 600;
        }
        
        .executive-summary {
            background: #f7fafc;
            padding: 30px;
            border-radius: 8px;
            border-left: 4px solid #667eea;
            white-space: pre-line;
            font-family: 'Monaco', 'Menlo', 'Consolas', monospace;
            font-size: 13px;
            line-height: 1.8;
            color: #2d3748;
        }
        
        .findings-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        
        .findings-table th {
            background: #2d3748;
            color: white;
            padding: 12px;
            text-align: left;
            font-weight: 600;
            font-size: 14px;
        }
        
        .findings-table td {
            padding: 12px;
            border-bottom: 1px solid #e2e8f0;
            font-size: 14px;
        }
        
        .findings-table tr:hover {
            background: #f7fafc;
        }
        
        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 4px;
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .badge-critical {
            background: #c53030;
            color: white;
        }
        
        .badge-high {
            background: #dd6b20;
            color: white;
        }
        
        .badge-medium {
            background: #d69e2e;
            color: white;
        }
        
        .badge-low {
            background: #38a169;
            color: white;
        }
        
        .badge-info {
            background: #3182ce;
            color: white;
        }
        
        .badge-confirmed {
            background: #2f855a;
            color: white;
        }
        
        .badge-firm {
            background: #d69e2e;
            color: white;
        }
        
        .badge-tentative {
            background: #718096;
            color: white;
        }
        
        .url-cell {
            max-width: 400px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            color: #3182ce;
        }
        
        .finding-detail {
            margin-top: 40px;
            padding: 20px;
            background: #f7fafc;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }
        
        .finding-detail h3 {
            font-size: 18px;
            margin-bottom: 15px;
            color: #2d3748;
        }
        
        .finding-detail .detail-grid {
            display: grid;
            grid-template-columns: 150px 1fr;
            gap: 10px;
            margin-bottom: 15px;
        }
        
        .finding-detail .detail-label {
            font-weight: 600;
            color: #4a5568;
        }
        
        .finding-detail .detail-value {
            color: #2d3748;
        }
        
        .finding-detail .description {
            margin-top: 15px;
            padding: 15px;
            background: white;
            border-radius: 4px;
            line-height: 1.8;
        }
        
        .finding-detail .steps {
            margin-top: 15px;
        }
        
        .finding-detail .steps ol {
            padding-left: 20px;
            line-height: 2;
        }
        
        .finding-detail .evidence {
            margin-top: 15px;
        }
        
        .finding-detail .evidence ul {
            padding-left: 20px;
            line-height: 2;
        }
        
        footer {
            background: #2d3748;
            color: white;
            padding: 20px 40px;
            text-align: center;
            font-size: 14px;
        }
        
        footer a {
            color: #667eea;
            text-decoration: none;
        }
        
        .no-findings {
            text-align: center;
            padding: 60px 20px;
            color: #718096;
        }
        
        .no-findings .icon {
            font-size: 48px;
            margin-bottom: 20px;
        }
        
        .no-findings .message {
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Security Assessment Report</h1>
            <div class="subtitle">Target: {{ run.target }} | Generated: {{ report.generated_at|format_datetime }}</div>
        </header>
        
        <div class="content">
            <!-- Scan Information -->
            <section class="section">
                <h2 class="section-title">Scan Information</h2>
                <div class="info-grid">
                    <div class="info-card">
                        <div class="label">Target</div>
                        <div class="value">{{ run.target }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Profile</div>
                        <div class="value">{{ run.profile }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Engagement Mode</div>
                        <div class="value">{{ run.engagement_mode.value }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Status</div>
                        <div class="value">{{ run.state.value }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Duration</div>
                        <div class="value">{{ run.duration|format_duration }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Scan Date</div>
                        <div class="value">{{ run.created_at|format_datetime }}</div>
                    </div>
                </div>
            </section>
            
            <!-- Statistics -->
            <section class="section">
                <h2 class="section-title">Findings Summary</h2>
                <div class="stats-grid">
                    <div class="stat-card critical">
                        <div class="count">{{ stats.critical_count }}</div>
                        <div class="label">Critical</div>
                    </div>
                    <div class="stat-card high">
                        <div class="count">{{ stats.high_count }}</div>
                        <div class="label">High</div>
                    </div>
                    <div class="stat-card medium">
                        <div class="count">{{ stats.medium_count }}</div>
                        <div class="label">Medium</div>
                    </div>
                    <div class="stat-card low">
                        <div class="count">{{ stats.low_count }}</div>
                        <div class="label">Low</div>
                    </div>
                    <div class="stat-card info">
                        <div class="count">{{ stats.info_count }}</div>
                        <div class="label">Info</div>
                    </div>
                </div>
                
                <div class="info-grid">
                    <div class="info-card">
                        <div class="label">Total Findings</div>
                        <div class="value">{{ stats.total_findings }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Unique Hosts</div>
                        <div class="value">{{ stats.unique_hosts }}</div>
                    </div>
                    <div class="info-card">
                        <div class="label">Unique URLs</div>
                        <div class="value">{{ stats.unique_urls }}</div>
                    </div>
                </div>
            </section>
            
            <!-- Executive Summary -->
            <section class="section">
                <h2 class="section-title">Executive Summary</h2>
                <div class="executive-summary">{{ executive_summary }}</div>
            </section>
            
            <!-- Findings -->
            <section class="section">
                <h2 class="section-title">Detailed Findings</h2>
                
                {% if findings %}
                <table class="findings-table">
                    <thead>
                        <tr>
                            <th>Severity</th>
                            <th>Type</th>
                            <th>Title</th>
                            <th>Host</th>
                            <th>Parameter</th>
                            <th>Tool</th>
                            <th>Confidence</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for finding in findings %}
                        <tr>
                            <td>
                                <span class="badge {{ finding.severity.value|severity_badge_class }}">
                                    {{ finding.severity.value }}
                                </span>
                            </td>
                            <td>{{ finding.type }}</td>
                            <td>{{ finding.title }}</td>
                            <td>{{ finding.host }}</td>
                            <td>{{ finding.parameter or 'N/A' }}</td>
                            <td>{{ finding.tool }}</td>
                            <td>
                                <span class="badge {{ finding.confidence.value|confidence_badge_class }}">
                                    {{ finding.confidence.value }}
                                </span>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
                
                <!-- Detailed Finding Information -->
                {% for finding in findings %}
                <div class="finding-detail">
                    <h3>{{ finding.title }}</h3>
                    
                    <div class="detail-grid">
                        <div class="detail-label">Severity:</div>
                        <div class="detail-value">
                            <span class="badge {{ finding.severity.value|severity_badge_class }}">
                                {{ finding.severity.value }}
                            </span>
                        </div>
                        
                        <div class="detail-label">Confidence:</div>
                        <div class="detail-value">
                            <span class="badge {{ finding.confidence.value|confidence_badge_class }}">
                                {{ finding.confidence.value }}
                            </span>
                        </div>
                        
                        <div class="detail-label">Type:</div>
                        <div class="detail-value">{{ finding.type }}</div>
                        
                        <div class="detail-label">URL:</div>
                        <div class="detail-value">
                            <a href="{{ finding.url }}" target="_blank">{{ finding.url }}</a>
                        </div>
                        
                        <div class="detail-label">Host:</div>
                        <div class="detail-value">{{ finding.host }}</div>
                        
                        <div class="detail-label">Parameter:</div>
                        <div class="detail-value">{{ finding.parameter or 'N/A' }}</div>
                        
                        <div class="detail-label">Tool:</div>
                        <div class="detail-value">{{ finding.tool }}</div>
                        
                        <div class="detail-label">Timestamp:</div>
                        <div class="detail-value">{{ finding.timestamp|format_datetime }}</div>
                    </div>
                    
                    {% if finding.description %}
                    <div class="description">
                        <strong>Description:</strong><br>
                        {{ finding.description }}
                    </div>
                    {% endif %}
                    
                    {% if finding.reproduction_steps %}
                    <div class="steps">
                        <strong>Reproduction Steps:</strong>
                        <ol>
                            {% for step in finding.reproduction_steps %}
                            <li>{{ step }}</li>
                            {% endfor %}
                        </ol>
                    </div>
                    {% endif %}
                    
                    {% if finding.remediation %}
                    <div class="description">
                        <strong>Remediation:</strong><br>
                        {{ finding.remediation }}
                    </div>
                    {% endif %}
                    
                    {% if finding.evidence_paths %}
                    <div class="evidence">
                        <strong>Evidence Files:</strong>
                        <ul>
                            {% for evidence in finding.evidence_paths %}
                            <li>{{ evidence }}</li>
                            {% endfor %}
                        </ul>
                    </div>
                    {% endif %}
                    
                    {% if finding.references %}
                    <div class="evidence">
                        <strong>References:</strong>
                        <ul>
                            {% for reference in finding.references %}
                            <li><a href="{{ reference }}" target="_blank">{{ reference }}</a></li>
                            {% endfor %}
                        </ul>
                    </div>
                    {% endif %}
                </div>
                {% endfor %}
                
                {% else %}
                <div class="no-findings">
                    <div class="icon">✓</div>
                    <div class="message">No security findings identified during this scan.</div>
                </div>
                {% endif %}
            </section>
        </div>
        
        <footer>
            <p>Generated by GaleHunTUI - Automated Web Pentesting Tool</p>
            <p>Report ID: {{ run.id }}</p>
        </footer>
    </div>
</body>
</html>
